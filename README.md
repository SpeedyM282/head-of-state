# Президент — сатирический политический симулятор

Мобильная игра в духе Plague Inc.: игрок правит вымышленной страной, балансируя между народом, элитами и казной. Вектор правления (демократия ↔ тоталитаризм) складывается из решений игрока и меняет правила игры.

Документы: [GDD.md](./GDD.md) · [ARCHITECTURE.md](./ARCHITECTURE.md) · [CLAUDE.md](./CLAUDE.md) (правила для Claude Code)

## Стек

React 18 · TypeScript (strict) · Vite · Zustand · Tailwind v4 · Vitest · Capacitor (этап 4)

## Команды

```bash
npm install        # зависимости
npm run dev        # дев-сервер
npm test           # тесты, включая автоигрока (смотри статистику в выводе)
npm run typecheck  # проверка типов
npm run build      # прод-сборка
```

## Статус

- [x] Этап 1 — симуляционное ядро (`src/core/`): tick, события, вектор, 4 поражения, детерминизм
- [x] Этап 2 (частично) — контент: 15 реформ, 22 события (цель ~30, см. TODO в `src/data/events/`)
- [x] Этап 3 — UI: меню, главный экран, реформы, события, конец игры
- [ ] Этап 4 — Capacitor + Android-сборка (persistence уже изолирован в `src/store/persistence.ts`)
- [ ] Этап 5 — плейтест и баланс (`src/data/balance.ts`)
